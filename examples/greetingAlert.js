// This script shows a greeting message in an alert box.

// Define a message
var message = "hello";

// Define the user's name
var name = "aydin";

// Show a combined greeting using alert
alert(message + " there, " + name);