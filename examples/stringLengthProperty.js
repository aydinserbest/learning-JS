// This script gets the length of a string using the .length property.

// Define a name as a string
var name = "angela";

// Access the length property (number of characters in the string)
console.log(name.length);

// Note: .length is a property, not a function, so no parentheses are used